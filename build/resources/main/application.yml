spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://3.39.237.132:3306/CertiMate?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: certimate
    # 🚨 깃허브에 올릴 때는 진짜 비밀번호를 지우고 가짜 문자로 바꿔서 올리세요!
    password: CertiMate!2026App

  jpa:
    hibernate:
    properties:
      ddl-auto: update
      hibernate:
        format_sql: true
    show-sql: true

# 🚨 [캡스톤 추가] 카카오 로그인 설정
kakao:
  client-id: 3fc4df0a3500e172f4eb5ef1b83f87e5
  redirect-uri: http://localhost:3000/auth/kakao/callback